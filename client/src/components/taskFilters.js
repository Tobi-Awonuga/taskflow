// TODO: Render filter controls (status, priority, assignee, date range)
// Props: current filters object, onChange callback
export function renderTaskFilters(filters = {}, onChange = () => {}) {
  return `<div>TODO: filter controls (status, priority, assignee, date)</div>`;
}
